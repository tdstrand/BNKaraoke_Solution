<Window x:Class="BNKaraoke.DJ.Views.VideoPlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
        xmlns:overlays="clr-namespace:BNKaraoke.DJ.Views.Overlays"
        xmlns:viewModels="clr-namespace:BNKaraoke.DJ.ViewModels.Overlays"
        xmlns:controls="clr-namespace:BNKaraoke.DJ.Controls"
        mc:Ignorable="d"
        Title="BNKaraoke.com"
        WindowState="Maximized"
        WindowStyle="None"
        WindowStartupLocation="Manual"
        Background="{DynamicResource WindowBackground}">
    <Window.Resources>
        <LinearGradientBrush x:Key="WindowBackground" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#1e3a8a" Offset="0"/>
            <GradientStop Color="#3b82f6" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="LeftEdgeBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#CC1e3a8a" Offset="0"/>
            <GradientStop Color="#003b82f6" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="RightEdgeBrush" StartPoint="1,0" EndPoint="0,0">
            <GradientStop Color="#CC1e3a8a" Offset="0"/>
            <GradientStop Color="#003b82f6" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>
    <Grid Background="{StaticResource WindowBackground}"
          DataContext="{x:Static viewModels:OverlayViewModel.Instance}"
          SnapsToDevicePixels="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="{Binding TopRowHeight}"/>
            <RowDefinition Height="{Binding CenterRowHeight}"/>
            <RowDefinition Height="{Binding BottomRowHeight}"/>
        </Grid.RowDefinitions>

        <overlays:OverlayBand Grid.Row="0"
                              Panel.ZIndex="10"
                              Background="{Binding TopBandBrush}">
            <overlays:OverlayBand.Style>
                <Style TargetType="overlays:OverlayBand">
                    <Setter Property="Visibility" Value="{Binding TopBandVisibility}"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsBlueState}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </overlays:OverlayBand.Style>
            <controls:MarqueePresenter DebugName="Top-Playback"
                                       Panel.ZIndex="10"
                                       IsHitTestVisible="False"
                                       Text="{Binding TopBandText}"
                                       FontFamily="{Binding FontFamily}"
                                       FontSize="{Binding FontSize}"
                                       FontWeight="{Binding FontWeight}"
                                       Foreground="{Binding FontBrush}"
                                       StrokeEnabled="{Binding IsStrokeEnabled}"
                                       ShadowEnabled="{Binding IsShadowEnabled}"
                                       MarqueeEnabled="{Binding MarqueeEnabled}"
                                       MarqueeSpeedPxPerSec="{Binding MarqueeSpeedPxPerSecond}"
                                       SpacerWidthPx="{Binding MarqueeSpacerWidthPx}"
                                       CrossfadeMs="{Binding MarqueeCrossfadeMs}"
                                       Margin="0"
                                       HorizontalAlignment="Stretch"
                                       VerticalAlignment="Center"/>
        </overlays:OverlayBand>

        <Grid Grid.Row="1"
              x:Name="VideoHost"
              Background="#000000"
              ClipToBounds="True"
              FocusVisualStyle="{x:Null}"
              Focusable="False"
              SnapsToDevicePixels="True"
              UseLayoutRounding="True">
            <vlc:VideoView x:Name="VideoPlayer"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           Margin="0"
                           Background="#000000"
                           Focusable="False"
                           FocusVisualStyle="{x:Null}"
                           SnapsToDevicePixels="True"
                           UseLayoutRounding="True"/>
            <Border x:Name="LeftEdgeOverlay"
                    Background="{StaticResource LeftEdgeBrush}"
                    Width="180"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Stretch"
                    IsHitTestVisible="False"
                    Panel.ZIndex="1"/>
            <Border x:Name="RightEdgeOverlay"
                    Background="{StaticResource RightEdgeBrush}"
                    Width="180"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Stretch"
                    IsHitTestVisible="False"
                    Panel.ZIndex="1"/>
        </Grid>

        <overlays:OverlayBand Grid.Row="2"
                              Panel.ZIndex="10"
                              Background="{Binding BottomBandBrush}">
            <overlays:OverlayBand.Style>
                <Style TargetType="overlays:OverlayBand">
                    <Setter Property="Visibility" Value="{Binding BottomBandVisibility}"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsBlueState}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </overlays:OverlayBand.Style>
            <controls:MarqueePresenter DebugName="Bottom-Playback"
                                       Panel.ZIndex="10"
                                       IsHitTestVisible="False"
                                       Text="{Binding BottomBandText}"
                                       FontFamily="{Binding FontFamily}"
                                       FontSize="{Binding FontSize}"
                                       FontWeight="{Binding FontWeight}"
                                       Foreground="{Binding FontBrush}"
                                       StrokeEnabled="{Binding IsStrokeEnabled}"
                                       ShadowEnabled="{Binding IsShadowEnabled}"
                                       MarqueeEnabled="{Binding MarqueeEnabled}"
                                       MarqueeSpeedPxPerSec="{Binding MarqueeSpeedPxPerSecond}"
                                       SpacerWidthPx="{Binding MarqueeSpacerWidthPx}"
                                       CrossfadeMs="{Binding MarqueeCrossfadeMs}"
                                       Margin="0"
                                       HorizontalAlignment="Stretch"
                                       VerticalAlignment="Center"/>
        </overlays:OverlayBand>

        <!-- BLUE OVERLAY spans all rows so bands align perfectly with top/bottom -->
        <Grid x:Name="BlueOverlay"
              Grid.RowSpan="3"
              Panel.ZIndex="30"
              IsHitTestVisible="False"
              Background="{StaticResource WindowBackground}"
              DataContext="{x:Static viewModels:OverlayViewModel.Instance}">

            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsBlueState}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <Grid.RowDefinitions>
                <RowDefinition Height="{Binding TopRowHeight}"/>
                <RowDefinition Height="{Binding CenterRowHeight}"/>
                <RowDefinition Height="{Binding BottomRowHeight}"/>
            </Grid.RowDefinitions>

            <!-- TOP marquee (Blue screen) -->
            <overlays:OverlayBand Grid.Row="0" Panel.ZIndex="10" Background="{Binding TopBandBrush}">
                <overlays:OverlayBand.Style>
                    <Style TargetType="overlays:OverlayBand">
                        <Setter Property="Visibility" Value="{Binding TopBandVisibility}"/>
                    </Style>
                </overlays:OverlayBand.Style>
                <controls:MarqueePresenter DebugName="Top-Blue"
                                           Panel.ZIndex="10"
                                           IsHitTestVisible="False"
                                           Text="{Binding TopBandText}"
                                           FontFamily="{Binding FontFamily}"
                                           FontSize="{Binding FontSize}"
                                           FontWeight="{Binding FontWeight}"
                                           Foreground="{Binding FontBrush}"
                                           StrokeEnabled="{Binding IsStrokeEnabled}"
                                           ShadowEnabled="{Binding IsShadowEnabled}"
                                           MarqueeEnabled="{Binding MarqueeEnabled}"
                                           MarqueeSpeedPxPerSec="{Binding MarqueeSpeedPxPerSecond}"
                                           SpacerWidthPx="{Binding MarqueeSpacerWidthPx}"
                                           CrossfadeMs="{Binding MarqueeCrossfadeMs}"
                                           Margin="0"
                                           HorizontalAlignment="Stretch"
                                           VerticalAlignment="Center"/>
            </overlays:OverlayBand>

            <!-- Center content (Brand / QR) -->
            <StackPanel Grid.Row="1"
                        Panel.ZIndex="0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <Image Source="pack://application:,,,/Assets/bnk-qr-code.png"
                       Width="200"
                       Height="200"
                       Margin="0,0,0,20"/>
                <TextBlock Text="{Binding Brand}"
                           FontSize="70"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           TextAlignment="Center"/>
            </StackPanel>

            <!-- BOTTOM marquee (Blue screen) -->
            <overlays:OverlayBand Grid.Row="2" Panel.ZIndex="10" Background="{Binding BottomBandBrush}">
                <overlays:OverlayBand.Style>
                    <Style TargetType="overlays:OverlayBand">
                        <Setter Property="Visibility" Value="{Binding BottomBandVisibility}"/>
                    </Style>
                </overlays:OverlayBand.Style>
                <controls:MarqueePresenter DebugName="Bottom-Blue"
                                           Panel.ZIndex="10"
                                           IsHitTestVisible="False"
                                           Text="{Binding BottomBandText}"
                                           FontFamily="{Binding FontFamily}"
                                           FontSize="{Binding FontSize}"
                                           FontWeight="{Binding FontWeight}"
                                           Foreground="{Binding FontBrush}"
                                           StrokeEnabled="{Binding IsStrokeEnabled}"
                                           ShadowEnabled="{Binding IsShadowEnabled}"
                                           MarqueeEnabled="{Binding MarqueeEnabled}"
                                           MarqueeSpeedPxPerSec="{Binding MarqueeSpeedPxPerSecond}"
                                           SpacerWidthPx="{Binding MarqueeSpacerWidthPx}"
                                           CrossfadeMs="{Binding MarqueeCrossfadeMs}"
                                           Margin="0"
                                           HorizontalAlignment="Stretch"
                                           VerticalAlignment="Center" />
            </overlays:OverlayBand>
        </Grid>
    </Grid>
</Window>
